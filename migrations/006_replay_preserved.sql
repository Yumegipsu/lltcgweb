-- Autosave vs permanent library for saved replays.
-- Column + backfill + index are applied in db.php (tcgDbEnsureColumn / tcgDbRunMigrationOnce).
-- This file exists only to record the migration version in schema_migrations.

SELECT 1;
